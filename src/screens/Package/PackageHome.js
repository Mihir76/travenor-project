import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const PackageHome = () => {
  return (
    <View>
      <Text>PackageHome</Text>
    </View>
  );
};

export default PackageHome;

const styles = StyleSheet.create({});
